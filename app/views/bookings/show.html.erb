<div class="container">
  <div class="row">
    <div class="col-xs-12 col-sm-12 text-center">
      <div class= "interpreter-sign-in">
        <div class="dashboard-bio-and-rating">
          <h1>Review of your booking with <%= @interpreter.name%></h1>
          <div class="container">
            <div class="row">
              <div class="col-xs-12 col-sm-6 text-right">
                <p>Start Time: <%= @booking.booking_date %></p>
                <p>Duration: <%= @booking.duration %></p>
              </div>
              <div class="col-xs-12 col-sm-6 text-left">
                <p>Location: <%= @booking.location %></p>
                <p>Price: $<%= (@interpreter.price.to_f  *  (@booking.duration.to_f / 3600))%></p>
              </div>
            </div>
          </div>
          <div class="padded-div-top">
            <p>If this information is correct, continue to <strong>payment</strong></p>
            <p>If this information is incorrect, please <strong><%= link_to "fix it", edit_interpreter_booking_path(@interpreter.id, @booking.id) %></strong></p>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
<div class="text-right padded-div-top text-center">
  <button type="button" class="btn-grey"><%= link_to "Back to Search", root_path%></button>
</div>
